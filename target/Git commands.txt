p
		
		git config --global user.email sivaneshkasi@gmail.com
		git config --global user.name sivaneshkasi
		git init
		git remote add origin url
		git add.
		git commit -m "file upload successfull"
		git push origin master
		
		
		git clone url
		
		git fetch
		git checkout Copy
		git add .
		git commit -m "created demo class"
		git push origin Copy
		
		
		git pull origin master 
		git add .
		git commit -m "refreshed with recent codes"
		git push origin copy
		
	}

}
